{
  "name": "classme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build-sass-prod && concurrently --kill-others \"npm run json-server\" \"npm run watch-sass-dev\"",
    "prod": "npm run build-sass-prod && npm run json-server",
    "json-server": "json-server --watch db.json --port 3000",
    "watch-sass-dev": "node-sass --watch --update --style=expanded styles/scss -o assets/style",
    "build-sass-prod": "node-sass --style=compressed --include-path scss styles/scss -o assets/style"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brendobc/classMe.git"
  },
  "author": "Brendo",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/brendobc/classMe/issues"
  },
  "homepage": "https://github.com/brendobc/classMe#readme",
  "dependencies": {
    "concurrently": "^7.0.0",
    "node-sass": "^7.0.1"
  }
}